# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'rake', :task => 'build' do
  watch(%r{source\/.*\.coffee})
  watch(%r{source\/.*\.styl})
  watch(%r{source\/.*\.hamlc})
end
